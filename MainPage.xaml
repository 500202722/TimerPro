<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TimerPro.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0">
            
            <Label x:Name="lblDisplay" Text="00:00:00" FontSize="Title" Padding="0,75" HorizontalOptions="Center"/>
            <Button Text="Start" x:Name="btnStart" Clicked="BtnStart_OnClicked"/>
            <Button Text="Stop" x:Name="btnStop" Clicked="BtnStop_OnClicked"/>
            <Button Text="Reset" Clicked="BtnReset_OnClicked"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
